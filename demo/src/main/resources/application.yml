spring:
  application:
    name: DemoMicroservice
server:
  port : 8081
eureka:
    client:
        service-url:
            defaultZone: http://eurekaserver:8761/eureka
        fetch-registry: true
        register-with-eureka: true